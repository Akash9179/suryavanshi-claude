# Never sync personal or machine-specific data
memory/
projects/
settings.local.json
*.local.json

# Secrets
.env
.env.*
*.key
*.pem

# Per-machine opt-in to auto-pull (receiver machines only; created by install.sh)
.autopull

# OS / editor noise
.DS_Store
*.swp
.gstack/
